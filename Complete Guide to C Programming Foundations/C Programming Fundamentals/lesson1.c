#include <stdio.h>
// It it the include preprocessor directive, 
// It tells the compiler to fetch the c code written in stdio.h file
// stdio.h stands for standar input-output header file


// The C standard (C99 and later) specifies that if the main function does not explicitly return a value, it behaves as if it returned 0.
// This is a special rule for main. If another function without a return type omits return, the behavior is undefined.
int main(){

    int a, b;

    puts("Goodbye, to the cruel world again!");

}